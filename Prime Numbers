lownum = 500
upnum= 600

for prime in range(lownum, upnum + 1):
   
   if prime > 1:
       for i in range(2, prime):
           if (prime % i) == 0:
               break
       else:
        print(prime)
        
